% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{varma_residuals}
\alias{varma_residuals}
\title{VARMA residuals}
\usage{
varma_residuals(Y, Phi, Theta, intercept)
}
\arguments{
\item{Y}{\eqn{n \times m} matrix of observations (rows = time points).}

\item{Phi}{\eqn{m \times m \times p} cube of VAR coefficient matrices.
Pass \code{array(0, c(m, m, 0))} when \code{p = 0}.}

\item{Theta}{\eqn{m \times m \times q} cube of VMA coefficient matrices.
Pass \code{array(0, c(m, m, 0))} when \code{q = 0}.}

\item{intercept}{\eqn{m}-vector of intercepts. Pass \code{numeric(0)} to
omit the intercept.}
}
\value{
\eqn{n \times m} matrix of residuals.
}
\description{
Iterates the VARMA(p, q) recursion forward to produce one-step-ahead
residuals.  The first \code{max(p, q)} rows of the output are zero
(pre-sample); valid residuals begin at row \code{max(p, q) + 1} (1-indexed).
The AR part conditions on the observed \code{Y[1:p, ]}; pre-sample errors
used by the MA part are initialised to zero.
}
