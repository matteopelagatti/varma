% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{autocov_mi_cpp}
\alias{autocov_mi_cpp}
\title{Compute theoretical autocovariance matrices for a VARMA(p,q) process
using the method of Mittnik (1990). Identical call convention to
\code{autocov_gs_cpp}: \code{ma_in} holds B_1,...,B_q only; B_0 = I is implicit.}
\usage{
autocov_mi_cpp(ar_in, ma_in, cov, max_lag = 0L)
}
\arguments{
\item{ar_in}{3D array (m x m x p) of AR coefficients A_1,...,A_p, or NULL.}

\item{ma_in}{3D array (m x m x q) of MA coefficients B_1,...,B_q, or NULL.}

\item{cov}{Innovation covariance matrix Sigma (m x m).}

\item{max_lag}{Number of lags to return beyond lag 0.}
}
\value{
3D array (m x m x (max_lag+1)); slice k+1 (1-based in R) is Gamma_k.
}
\description{
Compute theoretical autocovariance matrices for a VARMA(p,q) process
using the method of Mittnik (1990). Identical call convention to
\code{autocov_gs_cpp}: \code{ma_in} holds B_1,...,B_q only; B_0 = I is implicit.
}
\examples{
\dontrun{
sourceCpp("mittnik1990_acov.cpp")

# Univariate AR(1): Gamma_k = phi^k / (1 - phi^2)
G <- varma_acov(array(0.7, c(1,1,1)), NULL, matrix(1), max_lag = 5)

# Bivariate VARMA(1,1)
m  <- 2
A1 <- matrix(c(0.4, 0.2, 0.1, 0.3), m, m)
B1 <- matrix(c(0.2, 0.1, 0.0, 0.2), m, m)
G  <- varma_acov(array(A1, c(m,m,1)), array(B1, c(m,m,1)), diag(m), max_lag = 8)
G[,,1]  # Gamma_0 (symmetric)
G[,,2]  # Gamma_1
}
}
